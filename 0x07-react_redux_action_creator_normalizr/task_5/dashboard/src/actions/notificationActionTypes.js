// Action type constant to be used when marking a notification as read
export const MARK_AS_READ = 'MARK_AS_READ';

// Action type constant used when setting the type filter for notifications
export const SET_TYPE_FILTER = 'SET_TYPE_FILTER';

// Object representing available notification type filters
export const NotificationTypeFilters = {
    DEFAULT: 'DEFAULT',
    URGENT: 'URGENT',
};
